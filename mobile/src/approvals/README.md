# approvals/

**`approvalStatus.ts`** — trivial constants: the `ApprovalStatus` type (`Pending`/`Approved`/
`Rejected`), `APPROVAL_STATUS_COLORS`, and `APPROVAL_STATUS_FILTERS` (includes a leading `'All'`),
used by the Approvals tab's status badges and filter chips.
