# catalog/

**`catalogItemType.ts`** — re-exports `formatMoney` from `../common/format.ts`, plus
`CATALOG_ITEM_TYPE_LABELS`, `CATALOG_ITEM_TYPE_COLORS`, and a `CATALOG_ITEM_TYPES` array for the 3
`CatalogItemType` values (`Stock`/`TimeBased`/`Quote`), used by the catalog list and item-edit
screens' type badges/chips.
